export type NEWS_ONE = {
  id: number;
  title: string;
  text: string;
  date: string;
  timeToRead: string;
  image: string;
  is_top: boolean;
};

export type SEARCH_ITEM = {
  id: number;
  title: string;
};
